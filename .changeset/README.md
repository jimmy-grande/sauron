# Changesets

This project uses [Changesets](https://github.com/changesets/changesets) to manage releases.

Run `pnpm changeset` to create a new changeset. When changes are merged to `main`, a GitHub
workflow will create or update a release pull request.
